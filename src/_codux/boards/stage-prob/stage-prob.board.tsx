import { createBoard } from '@wixc3/react-board';
import { StageProb } from '../../../components/stage-prob/stage-prob';

export default createBoard({
    name: 'StageProb',
    Board: () => <StageProb> childrennnnn </StageProb>,
    isSnippet: true,
});